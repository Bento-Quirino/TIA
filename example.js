// Variável da pergunta analisada
const question = 'quaL_tiMe_vOcê_torce?"

// toUppweCase, toLowerCase
console.log(question.toUpperCase()) // Deixa tudo em CAIXA ALTA
console.log(question.toLowerCase()) // Deixa tudo em minusculo
// Mostra no terminal
console.log(question) // Printa no terminal

// Split
console.log(question.split("_")) // Separa onde tem "_"

// Verifica se é pergunta

// Supõe o que é a pergunta

// Trata a resposta

// Monta a pergunta
